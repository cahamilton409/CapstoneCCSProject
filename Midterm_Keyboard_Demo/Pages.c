#include "driverlib.h"

void Spanish_Home(char[] characters) {
    int16_t[] X_coords;
    int16_t[] Y_coords;
    int16_t length;
    Create_Grid_Given_Points(X_coords, Y_coords, length, characters)
}

void Spanish_Pressed(int16_t press_location) {
    int16_t[] X_coords;
    int16_t[] Y_coords;
    int16_t length;
    Create_Grid_Given_Points(X_coords, Y_coords, length, characters)
    // ADD PRESSED KEY DARKNESS
}

void French_Home(char[] characters) {
    int16_t[] X_coords;
    int16_t[] Y_coords;
    int16_t length;
    Create_Grid_Given_Points(X_coords, Y_coords, length, characters)
}

void French_Pressed(int16_t press_location) {
    int16_t[] X_coords;
    int16_t[] Y_coords;
    int16_t length;
    Create_Grid_Given_Points(X_coords, Y_coords, length, characters)
    // ADD PRESSED KEY DARKNESS
}
